# Copy to .env (gitignored) and fill in. Never commit real values.
GEMINI_API_KEY=
# Dashboard credentials (used from Phase: dashboard onward)
DASH_USERNAME=autoace
DASH_PASSWORD=
# Hugging Face token with pyannote/segmentation-3.0 terms accepted (overlap detection)
HF_TOKEN=
